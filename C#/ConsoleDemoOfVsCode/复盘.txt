2024-2-1:
    1. 多核cpu，多核多cpu：比如单cpu48核心和双cpu48核心，前者的制作难度和成本远大于后者，而且前者的性能更强
    2. 多线程：不是并行，你可以在单核cpu中实现多线程，但是不可以实现并行，只有多核cpu才可以实现并行，而单核cpu多线程是通过分配时间片实现的
    3. 并行：同一时刻有多个线程同时执行不同的任务(一个核心处理一个线程)
    4. 并发：通过os的调度，分配时间片形式执行
    5. 对象初始化器，对象初始化器匿名对象


2024-2-2：
    1. 关系型数据库RDBMS 和 非关系型数据库NoSQL
    2. L2S： 